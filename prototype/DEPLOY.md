# Chạy thử

Mở `prototype/index.html` bằng trình duyệt hoặc cấu hình GitHub Pages trỏ đến thư mục `prototype` sau khi hợp nhất nhánh.

Bản đồ nền cần kết nối Internet để tải Leaflet và OpenStreetMap.
